# 容器化配置
# =============================================================================
# 使用 Python 3.9 官方镜像（与你的本地环境一致）
# =============================================================================
FROM python:3.9-slim
# python:3.9-slim 是轻量版 Python 3.9 镜像
# slim 版本体积小，适合生产环境

# =============================================================================
# 设置工作目录
# =============================================================================
WORKDIR /code
# 容器内的当前工作目录设置为 /code
# 后续 COPY 和 RUN 命令都在这个目录下执行

# =============================================================================
# 复制依赖文件
# =============================================================================
COPY requirements.txt .
# 将本地的 requirements.txt 复制到容器的 /code 目录
# . 表示当前工作目录（即 /code）

# =============================================================================
# 安装 Python 依赖
# =============================================================================
RUN pip install --no-cache-dir -r requirements.txt
# --no-cache-dir 不缓存安装包，减小镜像体积
# -r requirements.txt 安装依赖列表中所有包

# =============================================================================
# 复制项目代码
# =============================================================================
COPY ./app ./app
# 将本地的 app 文件夹复制到容器的 /code/app 目录


# =============================================================================
# 暴露端口
# =============================================================================
EXPOSE 8000
# 告诉 Docker 这个容器会监听 8000 端口
# 注意：这只是声明，实际映射需要在 docker run 时指定

# =============================================================================
# 启动命令
# =============================================================================
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
# CMD 是容器启动时执行的命令
# 使用列表格式（推荐），每个参数是列表的一个元素
# 启动 uvicorn 服务器，运行 app.main 中的 app 实例